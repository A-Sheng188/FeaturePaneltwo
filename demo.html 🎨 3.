<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <title>FeaturePanel演示</title>
    <script src="https://unpkg.com/react@18/umd/react.development.js"></script>
    <script src="https://unpkg.com/react-dom@18/umd/react-dom.development.js"></script>
    <script src="https://unpkg.com/@babel/standalone/babel.min.js"></script>
    <style>
        body { font-family: Arial; padding: 20px; }
        .demo-container { max-width: 800px; margin: 0 auto; }
        .component-area { border: 2px dashed #ccc; padding: 20px; margin: 20px 0; }
    </style>
</head>
<body>
    <div class="demo-container">
        <h1>🎯 FeaturePanel 演示页面</h1>
        <p>这是一个可复用的UI组件演示</p>
        
        <div class="component-area">
            <h3>组件展示区域：</h3>
            <div id="root"></div>
        </div>
        
        <div style="margin-top: 30px; padding: 15px; background: #f0f9ff; border-radius: 8px;">
            <h3>📋 完成清单：</h3>
            <ul>
                <li>✅ 创建了GitHub仓库</li>
                <li>✅ 设置了分支保护</li>
                <li>✅ 配置了自动化流水线</li>
                <li>✅ 创建了可复用组件</li>
            </ul>
        </div>
    </div>

    <script type="text/babel">
        const FeaturePanel = () => (
            <div style={{border: '1px solid #e0e0e0', padding: '20px', borderRadius: '8px'}}>
                <h2 style={{marginTop: 0}}>功能面板组件</h2>
                <div style={{display: 'flex', gap: '10px', flexWrap: 'wrap'}}>
                    <div style={{padding: '10px', background: '#f5f5f5', borderRadius: '4px'}}>
                        <strong>搜索功能</strong>
                        <p>关键词筛选</p>
                    </div>
                    <div style={{padding: '10px', background: '#f5f5f5', borderRadius: '4px'}}>
                        <strong>状态筛选</strong>
                        <p>All/Active/Disabled</p>
                    </div>
                    <div style={{padding: '10px', background: '#f5f5f5', borderRadius: '4px'}}>
                        <strong>卡片列表</strong>
                        <p>支持点击查看详情</p>
                    </div>
                </div>
                <button style={{marginTop: '15px', padding: '8px 16px', background: '#007acc', color: 'white', border: 'none', borderRadius: '4px'}}>
                    点击查看详情 →
                </button>
            </div>
        );
        
        ReactDOM.render(<FeaturePanel />, document.getElementById('root'));
    </script>
</body>
</html>
